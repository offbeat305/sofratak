// Business constants set by Zizo (see CLAUDE.md). DO NOT change without asking.

/** Flat diner-paid service fee per order, pickup or delivery. */
export const SERVICE_FEE_CENTS = 79;

/** Catering (future phase): 5% diner-paid service fee, capped at $50.00. */
export const CATERING_FEE_RATE = 0.05;
export const CATERING_FEE_CAP_CENTS = 5000;
